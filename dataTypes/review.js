// Variables
    // Variable decleration and syntax  
    // Give variables value of a string, object, or array
    // Re-assign variables
    // Understand hoisting 
    // Understand scope 
    // Options include: var, let, and const

// Numbers
    // Declare a number (hint: no quotes)
    // How to do basic math with number 
        // +, -, /, *, %
    // How to do complex math with numbers
        // Examples:
            // Math.round(n)
            // Math.abs(n)
            // Math.ceil(n)
            // Math.floor(n)
            // etc..

// Boolean
    // Comparisons
        // >, <, >=, <=, ===, ==, !==, !=
    // All values have either a truthy or falsy value
        // Check using this: (change the -1 for any other value)   
            // if(-1 == true){
            //     console.log(true);
            // } else {
            //     console.log(false);
            // }

// Strings
    // String definition & syntax
    // How to change a string (we can't)
    // Getting a specific value of a string using bracket notation str[i]
    // Using single quotes vs double quotes vs backticks (template literals)
    // Using interpolation, ${}, within template literals

    // Methods
        // get the length
        // get a specific character
        // get a specific sub-string 
        // get the index of a a character
        // replace the first occurance of a character
        // making the string upper or lower case

// Arrays
    // Array definition & syntax
    // How to change an array
    // Getting specific values of an array 
    // What can be contained in an array (hint: all other data types)

    // Methods 
        // Adding to the front of an array
        // Adding to the back of an array 
        // Adding to the middle of an array
            // replacing or swapping elements in an array
        // Removing from the front of an array
        // Removing from the back of an array
        // Removing from the middle of an array (use splice without adding an element) arr.splice(i,1)
        // Change an array into a string
        // Get the length of an array
        // Get the value of an element at a specific index in an array
        // Get a range of elements, for example elements 1-5 or 2-3

// Objects
    // Defining an object
    // Dot notation
        // adding to an object
        // changing a key-value pair in an object
        // accessing a key-value pair in an object

// Functions
    // syntax
    // parameters
    // arguments
    // return keyword
    // invoking
        // what invoking or calling a function means
        // how to invoke with different arguments (either variables or data directly invoked)

// General Data Type Stuff
    // Primative vs Non-Primative
    // Primative Data Types = null, undefined, 1, 'hello world', true, Symbol(thing)
    // Non-Primative Data Types = object, array, function
    // We know what console.log() does (just puts something on the terminal)