// instructions:
    // write functions that take in strings and produce results

// level 1:
    // return the first letter of the following strings
        // We are invoking the function twice, once with each string
        // only add code where there is the text "this is where your code goes"
var commonString = 'Hello World';
var uncommonString = 'Goodbye World';

function firstLetter (parameter) {
    return //this is where your code goes
}

console.log(firstLetter(commonString));
console.log(firstLetter(uncommonString));

// level 1:
    // return the second letter of the following strings
        // We are invoking the function twice, once with each string
        // only add code where there is the text "this is where your code goes"

var light = 'Daytime';
var dark = 'Nighttime';

function secondLetter (parameter) {
    return //this is where your code goes
}

console.log(secondLetter(light));
console.log(secondLetter(dark));

// level 2:
    // you may want multiple lines of code in these exercises
    // Feel free to write code where '// something' is written and
    // where  "this is where your code goes" is written

    // dynamically return the last letter of the following strings

var flag = 'Old Glory';
var country = 'United States';

function lastLetter (parameter) {
    //this is where your code goes
    return //something
}

console.log(lastLetter(flag));
console.log(lastLetter(country));

// level 2:
    // dynamically return the second-to-last letter of the following strings

var brand = 'Adidas';
var origin = 'Germany';

function lastLetter (parameter) {
    //this is where your code goes
    return //something
}

console.log(lastLetter(brand));
console.log(lastLetter(origin));

// level 3:
    // Remove all spaces from the following strings by using .split and .join

var greeting = "Hello my friend!"
var goodbye = "Hasta luego amigo!"

function noSpaces (parameter) {
    //this is where your code goes
    return //something
}

console.log(noSpaces(greeting));
console.log(noSpaces(goodbye));

// Remove all dashes from the following strings by using .split and .join

var increasing = "One-Two-Three-Four"
var decreasing = "Four-Three-Two-One"

function noDashes (parameter) {
    //this is where your code goes
    return //something
}

console.log(noSpaces(increasing));
console.log(noSpaces(decreasing));