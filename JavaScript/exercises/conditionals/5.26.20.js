// Medium:
    // If a string is longer than a second string return the value of the first string

// Medium-Hard:
    // If a string contains a vowel return 'We have a vowel here!'. It it doesn't return 'No vowels here!'.

// Hard
    // In an array of strings, compare the strings and return the longest
    // (You may want to use a loop and an if statement for this one)
